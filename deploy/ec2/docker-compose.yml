version: "3.8"

services:
  opioid-overdose-backend:
    container_name: opioid-overdose-backend
    build:
      context: ../..
      dockerfile: Dockerfile
    image: opioid-overdose-backend:latest
    ports:
      - "80:8080"
    environment:
      - PORT=8080
    restart: unless-stopped
